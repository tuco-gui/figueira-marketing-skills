# Generic adapter

Use only when no more specific adapter exists.

Do not pretend an unknown tool supports negative prompts, ingredients, keyframes, reference videos, camera controls or audio.

Produce a clean prompt based on modality, plus a short note listing assumptions that depend on the destination tool.
